<resources>

    <style name="AppTheme.AppCompat.Responsive" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="windowActionModeOverlay">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
</style>

    <style name="Theme.Buttons" parent="Theme.AppCompat">
        <item name="searchViewStyle">@style/ButtonsStyle</item>
    </style>

    <style name="ButtonsStyle" parent="Widget.AppCompat.SearchView">
    <!-- The layout for the search view. Be careful. -->
    <item name="layout">@drawable/agenda</item>

    </style>

</resources>
